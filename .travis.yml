language: python
python:
    - "3.6"
before_install:
    - pip install pytest pytest-cov
    - pip install coveralls
    - python setup.py install
script:
    - pytest -v --cov=wrfhydropy
after_success:
    - coveralls